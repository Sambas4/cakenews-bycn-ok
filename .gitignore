# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Capacitor — native folders are generated per-maintainer via `cap add`.
# The shared bits (capacitor.config.ts, plugins) are committed; the
# Xcode workspace and Gradle wrapper are not.
ios/
android/

# Local CI artefacts
coverage/
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
